// This component has been removed - pull-to-refresh functionality is no longer needed
// The scroll performance is already smooth without touch gestures

// Dummy export to prevent import errors during development
const PullToRefreshIndicator = () => null;

export default PullToRefreshIndicator;
